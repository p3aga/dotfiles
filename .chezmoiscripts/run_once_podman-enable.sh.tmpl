#!/bin/bash

{{ if or (stat "/.dockerenv") (stat "/run/.containerenv") }}
  echo "Container detected; skipping podman socket enablement"
  exit 0
{{ end }}

sudo systemctl enable --user --now podman.socket